#include <stdio.h>
#include <gmp.h>
#include <stdlib.h>
#include <string.h>

const int LEN_MAXN = 1500;
char M[LEN_MAXN]; // 待输入的消息

int main(){
    printf("请输入需要加密的密文 M：");
    scanf("%s", M);

    int mLen = strlen(M);
    int k = mLen / 8
    return 0;
}